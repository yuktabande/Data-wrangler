# This module will execute approved suggestions using pandas.
# For Phase 2, we only collect suggestions and custom input.
# Stubs provided for future expansion.

def execute_instruction(sheets: dict, instruction: dict):
    """
    Example:
      instruction = {
        "action": "merge",
        "sheet1": "Sales",
        "sheet2": "Returns",
        "on": "Order ID",
        "how": "inner"
      }
    """
    raise NotImplementedError("Phase 3 will implement this.")